# Session 2026-05-04 → 05

## מה נעשה

### 1. הסרת `pollingIntervalMin`
לאחר המעבר לפי fixed cron schedule (`0 13,20 * * *`), עמודת `pollingIntervalMin` הפכה למיותרת.

**שינויים:**
- Migration: `ALTER TABLE "BrokerConnection" DROP COLUMN IF EXISTS "pollingIntervalMin"`
- הסרה מ-Zod schema ומ-upsert ב-`app/api/ibkr/connect/route.ts`
- הסרה מה-SELECT ב-`app/api/ibkr/connection/route.ts`
- הסרת state, interface fields וה-`pollingIntervalMin` מה-request body ב-`app/(dashboard)/settings/page.tsx`
- הסרה מ-`components/sync-indicator.tsx` ו-`components/open-positions-dashboard.tsx`
- הסרה מה-SELECT ב-`app/(dashboard)/dashboard/page.tsx`
- רגנרוט `lib/db/types.ts`

---

### 2. לוגיקת נקודת הסנכרון — מבוסס סטטוס במקום זמן
הנקודה ב-`SyncIndicator` עברה ממדידת "כמה זמן עבר" ללוגיקת סטטוס + קוד שגיאה.

| צבע | מצב |
|---|---|
| ⚫ אפור | אף פעם לא סונכרן |
| 🟢 ירוק | הסנכרון האחרון הצליח (`SUCCESS`) |
| 🟡 כתום | שגיאה transient — הקרון הבא כנראה יתקן |
| 🔴 אדום | שגיאה fatal — דורש פעולת משתמש |

**קודי שגיאה transient (כתום):** 1001, 1004, 1005, 1006, 1007, 1008, 1009, 1017, 1018, 1019, 1021

**קודי שגיאה fatal (אדום):** 1003, 1010, 1011, 1012, 1013, 1014, 1015, 1016, 1020

מקור: [IBKR Flex Error Codes](https://www.ibkrguides.com/clientportal/performanceandstatements/flex3error.htm)

**גם תוקן:** `+ "Z"` ב-`formatShort` וב-`ibkrTitle` כדי שהזמן יוצג נכון (UTC→local).

---

### 3. תיקון IBKR Flex client — endpoint, header, polling
לאחר חקירה מעמיקה של הדוקומנטציה וניתוח לוגים, תוקנו מספר בעיות ב-`lib/ibkr/flex-client.ts`:

**שינויים:**
- `FLEX_REQUEST_URL` עודכן מה-endpoint הישן (`www.interactivebrokers.com/Universal/servlet/...`) לרשמי:
  `https://ndcdyn.interactivebrokers.com/AccountManagement/FlexWebService/SendRequest`
- נוסף `FLEX_HEADERS = { "User-Agent": "Java" }` — נדרש על ידי IBKR
- `MAX_RETRIES`: 5 → 30, `RETRY_DELAY_MS`: 2000 → 10_000 (polling כל 10s עד 5 דקות)
- Timeout error שונה ל-`IbkrTransientError` כדי ש-`lastSyncAt` לא יתעדכן בעת timeout
- הורחבו `IBKR_TRANSIENT_CODES` מ-3 קודים ל-11 (בהתאם לדוקומנטציה)

**סיבת הבעיה השורשית:** הטוקן שנוצר היה בסטטוס פגום ב-IBKR. יצירת טוקן חדש + ה-endpoint הנכון פתרו את הבעיה.

---

### 4. לוגים אבחוניים לאורך כל תהליך הסנכרון
נוספו `console.log` מפורטים ב-3 קבצים לצורך ניטור שוטף ואבחון:

**`lib/ibkr/flex-client.ts`:**
- Step 1: URL (token מוסתר) + referenceCode + url שהוחזרו
- Step 2: כל attempt עם URL, HTTP status, ו-300 תווים ראשונים של XML
- הודעה ברורה אם עדיין generating / ErrorCode / success

**`app/api/cron/ibkr-sync/route.ts`:**
- queryId בתחילת הריצה, xml.length אחרי fetchFlexQuery
- BrokerEvent id, מספר executions שנקלטו, תוצאות processExecutions
- lastSyncStatus בסיום

**`app/api/ibkr/connect/route.ts`:**
- queryId בשמירה, הודעה על תחילת background sync

---

## Commits
- `bfdf722` — Remove pollingIntervalMin; status-based sync dot color
- `f34f6b7` — Fix IBKR Flex endpoint URL, add User-Agent header, expand transient codes
- `ae489d9` — Fix Flex step-2 polling timeout: 12 retries × 10s (~120s total)
- `14fbebf` — Add diagnostic logging to IBKR Flex sync flow

---

## תוצאה מאומתת
הקרון עובד ✅ — מ-05/05/2026 הקרון רץ כסדרו ב-13:00 ו-20:00 UTC:
- `BrokerConnection.lastSyncStatus = SUCCESS`
- `BrokerConnection.accountId = U18277746`
- `BrokerEvent.processingStatus = PROCESSED`
- 54 executions (25 כפולים, 29 non-stock) — מבצע 2 ניסיונות Step 2 (~10 שניות)
