# Trade Analyst — Launch Window Security Audit & Remediation Plan

> **Audit date:** 2026-07-05
> **Auditor:** Claude (engineering:code-review skill)
> **App version reviewed:** main branch at commit `e5f3ccb` + production at https://tradeanalyst.app (Vercel)
> **Scope window:** commits `4a50ab7` → `6e0b209` (28–29 June 2026) + uncommitted launch-promo working-tree changes
> **Status:** ✅ COMPLETED 2026-07-08 — all 23 findings closed (X23 deferred by owner decision as future work with strikethrough Acceptance). All 8 open questions answered. No new findings discovered during remediation beyond X21/X22/X23 (already surfaced during audit writing and closed above). Re-open by changing Status to ACTIVE and adding new findings under "Discovered During Remediation".

---

## Background

Trade Analyst is a Hebrew-RTL trading journal with an AI assistant ("חנן"), built on Next.js 16 App Router + React 19 + Supabase, deployed on Vercel and going through a public launch. Users import trades from Interactive Brokers via Flex Web Service or manually, then get FIFO analytics, R-multiples, and Gemini-powered chat over their trade history. Over the 28–29 June window, three new platforms were wired in — Lemon Squeezy (billing, merchant-of-record), PostHog (product analytics + session replay), and Sentry (error monitoring) — alongside a Free/Pro tier gate, a public landing page + legal pages, security response headers, and a dynamic `/og` image route. This audit covers only what changed in that window and the data flows those changes introduced.

**Scope:** every commit between `4a50ab7` (Phase 1 close) and `6e0b209` (L19 doc note) plus the working-tree diff for the launch-promo discount work. Focused on: Lemon Squeezy checkout + webhook path, PostHog init and event flow, Sentry configuration, tier-gating in API routes, DB schema + RLS around the new billing columns, `proxy.ts` security headers + public route allowlist, the new `/og` and `/api/healthz` public endpoints. **Out of scope:** IBKR Flex integration internals, FIFO logic, chat/Gemini prompt handling, and the pre-existing auth flow — companion coverage lives in [`docs/completed/SECURITY-AUDIT.md`](../completed/SECURITY-AUDIT.md) (previous round) and [`docs/in-progress/LAUNCH-READINESS.md`](LAUNCH-READINESS.md) (rollout checklist).

**Stack reviewed:** Next.js 16 (App Router) · React 19 · Supabase (`@supabase/ssr` 0.6, `@supabase/supabase-js`) · Lemon Squeezy REST API v1 · posthog-js ^1.396 · @sentry/nextjs ^10.62 · Postgres 14.5 (Supabase managed) · Vercel Edge + Node runtimes · GitHub Actions cron.

**Methodology:**
1. Full read of `git log --stat` for the 28–29 June window and of every file touched by those commits.
2. Full read of every new or modified file under `app/api/billing/*`, `lib/billing/*`, `lib/analytics/*`, `sentry.*.config.ts`, `instrumentation*.ts`, `proxy.ts`, `app/api/healthz/route.ts`, `app/og/route.tsx`, `app/global-error.tsx`, `app/error.tsx`.
3. Cross-reference with the Pro-gated routes that consume `getUserTier`: `app/api/ibkr/connect/route.ts`, `app/api/chat/route.ts`, `app/api/export/activity-csv/route.ts`.
4. Direct Supabase MCP queries against production DB: `pg_policy` for RLS on `User`, `information_schema.column_privileges` and `information_schema.table_privileges` for grants on the new billing columns, `information_schema.columns` for column shapes.
5. Git history search (`git log --all --diff-filter=A -- .env*`) to confirm no `.env` file was ever committed to any branch.
6. Each finding scored internally with `Priority = (Impact + Risk) × (6 − Effort)`, 1–5 scale, for ordering only — scores not printed.

**Reference frameworks:** OWASP Top 10 (2021) · OWASP ASVS v4 L2 (payment & session-management chapters) · PCI SAQ-A (merchant-of-record scope) · CIS Postgres Benchmark (RLS + column privileges) · Sentry & PostHog privacy documentation for defaults · Next.js App Router security guidance · Lemon Squeezy webhook signing spec.

---

## How to Use This Plan

- Each finding is **outcome-driven**: it states what is wrong and what "fixed" looks like, but not how to implement it. The implementer chooses the approach.
- Work top-down by phase. **Do not skip Phase 1.** X1 and X2 are launch blockers — Pro tier is trivially bypassable today and PostHog is recording sensitive UI in production.
- Tick the box only when the **Acceptance** clause is satisfied — not when the change is merely written.
- When an item lands, flip `[ ]` → `[x]` on the heading line. Empty boxes mean unfinished; ticked boxes mean Acceptance met.
- If a finding turns out to be invalid in context, leave the box unchecked and add a `~~strikethrough~~` note explaining why.
- If a finding is deferred by an owner decision (won't-fix this round), tick `[x]`, wrap the Acceptance line in `~~strikethrough~~`, and append a short note explaining the decision.
- New findings discovered during fixes go at the bottom under "Discovered During Remediation".
- The `Status` line at the top is maintained by `/execute-work-plan`.
- Some findings overlap with [`docs/in-progress/LAUNCH-READINESS.md`](LAUNCH-READINESS.md) (specifically L8 Sentry + L16 PostHog + L21 billing). Where an item is called out in both files, fix once and tick the box in both.

---

## Strengths — What Already Works Well

Preserve these patterns when remediating:

- **Webhook HMAC uses constant-time comparison correctly.** `app/api/billing/webhook/route.ts:28-40` builds the expected signature with Node `createHmac`, then compares with `timingSafeEqual`, with a `try/catch` that safely handles length mismatches. This is the correct primitive.
- **All Pro-gated routes call `getUserTier` server-side, then return a structured 403.** `lib/billing/tier.ts:39-48` defines a single `proRequiredResponse(feature)` that every gate uses, so the client can uniformly detect `errorCode === 'pro_required'` and render the upgrade banner. Consistent pattern across `app/api/ibkr/connect/route.ts:32`, `app/api/chat/route.ts:41`, `app/api/export/activity-csv/route.ts:24`.
- **Sentry `sendDefaultPii: false` is set on all three runtimes** (`sentry.server.config.ts`, `sentry.edge.config.ts`, `instrumentation-client.ts`). Explicit opt-out on every entry point rather than relying on one config to cover everything.
- **`proxy.ts` public-route allowlist is exhaustive but tight.** The three new unauthenticated endpoints (`/og`, `/api/healthz`, `/api/billing/webhook`) are each explicitly listed by exact path — no `startsWith` wildcards that could be widened accidentally.
- **`lib/audit/log.ts:25-38` truncates IPs before storing them** (IPv4 → `/24`, IPv6 → first four groups). Privacy-conscious and unchanged in this window.
- **The webhook only writes via the service-role admin client** (`lib/supabase/admin.ts`), never via the anon-bound server client. Correct separation of trust boundaries.
- **PostHog + Sentry are hard-gated on `NODE_ENV === 'production'`.** Local dev and Vitest never emit — no accidental data leaks from staging noise or test runs.
- **The audit-log event enum was extended in step with the new billing writes.** `lib/audit/log.ts:4-12` now emits `tier_upgraded` / `tier_downgraded` / `subscription_updated`, so every state change from the webhook is observable in `AuditEvent`.
- **`.env.example` was updated for every new env var** (`LEMONSQUEEZY_*`, `NEXT_PUBLIC_POSTHOG_*`, `NEXT_PUBLIC_SENTRY_DSN`, `SENTRY_AUTH_TOKEN`, discount IDs). No documentation drift on the new secrets.
- **Git history is clean of secrets.** `git log --all --diff-filter=A -- .env .env.local .env.production` returns only `.env.example` — no real env file was ever committed to any branch, past or present.

---

## Findings

ID convention: `X##` numbered globally across phases. Where a finding was confirmed by reading production DB state or running the code path, the `Issue` line says **"Confirmed."**

---

### Phase 1 — Critical (Day-1 blockers, must clear before next release)

#### [x] X1. Paywall bypass — authenticated role can self-upgrade to Pro via direct Supabase UPDATE
- **Where:** DB grants + RLS on `public."User"` (introduced by migration `20260628064122_add_billing_columns_to_user`); consumed by [lib/billing/tier.ts:12-31](../../lib/billing/tier.ts:12) and every Pro-gated route.
- **Issue:** **Confirmed** against production via Supabase MCP. The `authenticated` role holds column-level `UPDATE` on `subscriptionTier`, `subscriptionStatus`, `subscriptionRenewsAt`, `lemonsqueezySubscriptionId`, and `lemonsqueezyCustomerId`. The single RLS policy on the `User` table is `users_own_row` with `polcmd='*'`, `USING (auth.uid() = id)`, `with_check = NULL`. Combined, that means any signed-in Free user can grant themselves Pro tier from a browser tab: `await supabase.from('User').update({ subscriptionTier: 'Pro' }).eq('id', user.id)`. The RLS check passes because it's their own row, and there is nothing else stopping the write. Every Pro gate ([app/api/ibkr/connect/route.ts:32](../../app/api/ibkr/connect/route.ts:32), [app/api/chat/route.ts:41](../../app/api/chat/route.ts:41), [app/api/export/activity-csv/route.ts:24](../../app/api/export/activity-csv/route.ts:24)) reads `subscriptionTier` back and trusts it. The result is a total defeat of the $14.99/mo paywall — including unlimited Gemini-2.5-pro calls with full trade history, which is the most expensive backend operation in the product. Same class of vulnerability also lets a user forge `lemonsqueezySubscriptionId` / `lemonsqueezyCustomerId` and confuse the webhook state machine on any subsequent legitimate event.
- **Acceptance:** Closed via two migrations on 2026-07-07:
  - `20260707185527_revoke_billing_column_grants_from_authenticated_anon` — the naive `REVOKE INSERT, UPDATE (cols)` from the original acceptance criterion. Postgres parses that as `REVOKE INSERT` (table-level) + `REVOKE UPDATE (cols)` (column-level); the INSERT part accidentally stripped all insert capability from `authenticated` (breaking signup) while the UPDATE (cols) part was silently shadowed by table-level UPDATE. Do not repeat this pattern.
  - `20260707190000_harden_user_billing_write_paths` — the correct fix. Restores INSERT scoped to signup-touched columns (`id, email, name, firstName, lastName, phone, addressStreet, addressCity, addressCountry, settings`) so `subscriptionTier` cannot be seeded to `'Pro'` at signup; revokes table-level UPDATE + DELETE + TRUNCATE + TRIGGER + REFERENCES from `authenticated`; re-grants UPDATE only on profile columns (`name, firstName, lastName, phone, address*, settings`); strips `anon` back to bare SELECT (RLS blocks its rows anyway). Service-role admin client is unaffected. Verified by [\_\_tests\_\_/integration/paywall-bypass.test.ts](../../__tests__/integration/paywall-bypass.test.ts) — 4/4 passing: (1) authenticated UPDATE on `subscriptionTier` is blocked, tier stays `Free`; (2) authenticated UPDATE on `lemonsqueezySubscriptionId` is blocked, stays `null`; (3) authenticated UPDATE on `firstName` still succeeds; (4) service-role admin can still UPDATE all billing columns (webhook path intact).

#### [x] X2. PostHog session recording enabled in production with no PII masking review
- **Where:** [lib/analytics/posthog.ts:9-18](../../lib/analytics/posthog.ts:9) and the identical inlined block in [components/posthog-provider.tsx:9-18](../../components/posthog-provider.tsx:9).
- **Issue:** `posthog.init(KEY, { capture_pageview: 'history_change', capture_pageleave: true, autocapture: true, disable_session_recording: false })` is what runs in production. No `session_recording` config block, no `mask_text_selector`, no `mask_all_text`, no `block_selector` — so the SDK falls back to its shipping defaults, which mask form inputs but do **not** mask rendered text or click-target labels. On this app that means every second of the `/research` dashboard (P&L per trade, tickers, position sizes, win rate, R-multiples, drawdown), the chat sidebar with חנן (full user+assistant conversation content), the trade-detail rows, and the profile screens (name, phone, address, broker connection status) get streamed to `us.i.posthog.com` as replayable DOM. `autocapture: true` additionally records every click with the element's visible text — clicking a trade row captures the ticker + P&L in the click event itself. This is data a competitor or acquirer would love, is not disclosed in the privacy policy in these terms, and is likely inconsistent with the GDPR data-minimisation stance the privacy page takes. Any users onboarded since PostHog was wired have already had sessions recorded.
- **Acceptance:** Closed 2026-07-07. Both PostHog init blocks ([lib/analytics/posthog.ts:16-24](../../lib/analytics/posthog.ts:16) + [components/posthog-provider.tsx:10-19](../../components/posthog-provider.tsx:10)) now set `disable_session_recording: true` and `autocapture: { css_selector_allowlist: ['[data-analytics]'] }`. Effect: no session replay is captured at all; autocapture is opt-in — no click event is recorded unless the target or an ancestor carries a `data-analytics` attribute (no element does today, so effectively autocapture is off, but the mechanism stays wired for future explicit opt-in). Existing `trackEvent()` funnel calls (`signup_started`, `email_confirmed`, `profile_completed`, `first_trade_imported`) continue emitting unchanged. Privacy page's existing PostHog disclosure ([app/(public)/privacy/page.tsx:114](../../app/(public)/privacy/page.tsx:114) — "אוסף נתוני שימוש אנונימיים") stays accurate with the new config, no copy change needed. Re-enable path with masking + sampling tracked as **X23**. **Owner action still required:** bulk-delete existing session recordings captured before this fix landed via the PostHog UI (Session Replay tab → filter by date range → bulk delete).

#### [x] X3. `/api/billing/checkout` has no rate limit — LS API key exhaustion + spend-side abuse vector
- **Where:** [app/api/billing/checkout/route.ts](../../app/api/billing/checkout/route.ts) whole route.
- **Issue:** Every other sensitive route calls `checkRateLimit(\`user:${userId}:...\`, ...)` (see `app/api/chat/route.ts:44` for the 30/hour chat cap). This route does not — an authenticated user can loop POST `/api/billing/checkout` and each call fires a signed request to `https://api.lemonsqueezy.com/v1/checkouts` with the owner's `LEMONSQUEEZY_API_KEY` in the `Authorization` header. Lemon Squeezy rate-limits per API key (120 req/min store-wide), so a single abusive account can exhaust the entire store's checkout capability for a minute at a time and rolling — effectively a payment-flow DoS. Discount IDs also get iterated through the endpoint during launch promo, so a bot could probe which discount combinations LS accepts and log the errors.
- **Acceptance:** Closed 2026-07-07. [app/api/billing/checkout/route.ts](../../app/api/billing/checkout/route.ts) now calls `checkRateLimit` twice before touching Lemon Squeezy — hourly bucket (`user:${id}:billing-checkout`, 10/hr) and daily bucket (`user:${id}:billing-checkout:daily`, 30/day). Each breach fires `logAuditEvent({ eventType: 'rate_limit_hit', metadata: { action: 'billing_checkout', bucket: 'hourly'|'daily' } })` and returns 429 via `rateLimitedResponse` with a `Retry-After` header. Verified by [\_\_tests\_\_/billing-checkout-rate-limit.test.ts](../../__tests__/billing-checkout-rate-limit.test.ts) — 4/4 passing: within-limit call returns 200 + LS URL; hourly bucket denial returns 429 with hourly audit; daily bucket denial returns 429 with daily audit; unauthenticated call returns 401 without touching the rate limiter.

---

### Phase 2 — Important (correctness & integrity)

#### [x] X4. Content-Security-Policy is Report-Only with no reporting endpoint — dead code
- **Where:** [proxy.ts:10-13](../../proxy.ts:10) — `Content-Security-Policy-Report-Only` header set on every response with `'unsafe-inline' 'unsafe-eval'` in `script-src` and no `report-uri` / `report-to`.
- **Issue:** Report-Only mode never blocks anything, and without a report endpoint, violations don't reach Sentry or anywhere else — the header emits pure noise. The current directive list also does not include the PostHog origin (`https://us.i.posthog.com` for XHR, `https://us-assets.i.posthog.com` for the bootstrap script) or the Vercel Analytics origins the site already uses, so the moment the header is switched to enforcing, PostHog stops loading and product analytics silently die. `frame-ancestors 'none'` is duplicated by `X-Frame-Options: DENY` but the CSP directive is the modern one that matters. `'unsafe-inline' 'unsafe-eval'` in `script-src` are Next.js requirements today but should carry a nonce eventually.
- **Acceptance:** Closed 2026-07-07. `Content-Security-Policy-Report-Only` block commented out in [proxy.ts:11-19](../../proxy.ts:11) with a `TODO(post-launch, X4)` comment referencing this finding — directive list preserved as a starting point for future revival. `X-Frame-Options: DENY` stays active as an equivalent frame guard with no ongoing cost. Verified post-deploy by `curl -I https://tradeanalyst.app/` returning no CSP header.

#### [x] X5. Webhook lacks event-id idempotency and silently no-ops on unknown user_id
- **Where:** [app/api/billing/webhook/route.ts:49-91](../../app/api/billing/webhook/route.ts:49).
- **Issue:** Two ops-hardening gaps. (a) The handler doesn't persist Lemon Squeezy's per-event id, so a re-delivered webhook re-executes the DB write. Reads are idempotent on terminal-state fields (`subscriptionTier`, `status`, `renews_at` — same input, same result), so it's low-risk today, but the moment ancillary logic is added (analytics events, email notifications, audit entries counted per event), duplicates become a real problem. (b) `.update(...).eq('id', userId)` silently returns success with zero rows updated when `user_id` in `custom_data` doesn't match a real user — could happen if a user deletes their account between checkout and the webhook, or if a test-mode event leaks to prod. The current code logs no warning and returns 200, so the state divergence between LS and this DB is invisible.
- **Acceptance:** Closed 2026-07-07. New `BillingWebhookEvent` table (migration `20260708090500_add_billing_webhook_event_table`) with `event_id TEXT UNIQUE` = `sha256(rawBody)` — stable across LS retries of the same delivery. Handler inserts before touching User; on `23505` returns `200 { ok:true, duplicate:true }` without re-writing state. Verified by X5 tests in [\_\_tests\_\_/billing-webhook.test.ts](../../__tests__/billing-webhook.test.ts). Zero-row-update path handled by X14 (below).

#### [x] X6. `isActiveStatus` grants Pro for `past_due` — indefinite failed-payment grace
- **Where:** [lib/billing/lemon-squeezy.ts:140-144](../../lib/billing/lemon-squeezy.ts:140).
- **Issue:** The active-tier check is `status === 'on_trial' || status === 'active' || status === 'past_due'`. `past_due` is Lemon Squeezy's "we tried to charge and it failed, we'll retry" state — it can persist for LS's full dunning window (~14 days on default settings, longer on custom). During that time the user keeps unlimited Pro access, including the most expensive backend operation (Gemini-2.5-pro chat with full history context). A user with a permanently invalid card can extract weeks of Pro-tier value before LS finally flips them to `unpaid` / `expired` and the next webhook downgrades them. The webhook only fires on transitions, so if LS never transitions, the user keeps Pro forever.
- **Acceptance:** Closed 2026-07-07. [lib/billing/lemon-squeezy.ts:150-152](../../lib/billing/lemon-squeezy.ts:150) — `isActiveStatus` now returns true only for `on_trial` and `active`. Regression covered by [\_\_tests\_\_/billing-active-status.test.ts](../../__tests__/billing-active-status.test.ts) — 9/9 passing: active/on_trial → Pro; past_due/paused/unpaid/cancelled/expired/empty/unknown/uppercase all → Free.

#### [x] X7. Sentry Replay Integration relies on undocumented defaults for text masking
- **Where:** [instrumentation-client.ts:1-13](../../instrumentation-client.ts:1) — `Sentry.init({ replaysOnErrorSampleRate: 1.0, replaysSessionSampleRate: 0, sendDefaultPii: false })` with no explicit `Sentry.replayIntegration({ ... })` call.
- **Issue:** `replaysOnErrorSampleRate: 1.0` captures 30 seconds of DOM every time an error hits. Sentry's shipping defaults happen to be `maskAllText: true` and `blockAllMedia: true`, but relying on them (a) means the config silently changes across SDK upgrades, and (b) leaves `networkDetailAllowUrls` and `networkCaptureBodies` at their defaults, which is fine today but not something the code is asserting. For a financial-data app the masking policy should be explicit and locked in, not inherited from whatever version of the SDK npm resolves.
- **Acceptance:** Closed 2026-07-07. [instrumentation-client.ts:11-19](../../instrumentation-client.ts:11) now passes `integrations: [Sentry.replayIntegration({ maskAllText: true, maskAllInputs: true, blockAllMedia: true, networkDetailAllowUrls: [] })]` explicitly. Masking policy is now locked in — cannot regress silently across SDK upgrades. Owner action: trigger one deliberate client error post-deploy and confirm in the Sentry Replays UI that the DOM shows only masked placeholders (no P&L, no tickers, no chat text).

#### [x] X8. Checkout does not validate `user.email` before passing to Lemon Squeezy
- **Where:** [app/api/billing/checkout/route.ts:49-55](../../app/api/billing/checkout/route.ts:49) — `userEmail: user.email ?? ''`.
- **Issue:** If `user.email` is falsy (edge cases in Supabase magic-link / OAuth flows where email is unverified or null on the user record), the checkout payload sends `checkout_data.email = ''`. Lemon Squeezy either rejects with a 422 (best case, user sees a generic "יצירת מנוי נכשלה" and can't proceed) or accepts and creates an orphan checkout with no email, breaking downstream receipt delivery. Either way, the user experience is undebuggable from their side.
- **Acceptance:** Closed 2026-07-07. [app/api/billing/checkout/route.ts:23-30](../../app/api/billing/checkout/route.ts:23) — if `user.email` is falsy (null or empty string), returns 400 with `{ error: 'כתובת מייל חסרה בחשבון. אנא השלם את פרטי הפרופיל לפני הרשמה למנוי.' }` before rate limits, config, or LS call. Downstream `createCheckoutSession` now trusts `user.email` (no `?? ''` fallback). Verified by two new cases in [\_\_tests\_\_/billing-checkout-rate-limit.test.ts](../../__tests__/billing-checkout-rate-limit.test.ts) covering `email: null` and `email: ''` — both return 400 with the Hebrew error, and neither touches the rate limiter or LS.

#### [x] X9. Webhook does not shape-check `user_id` before using it as a Postgres UUID
- **Where:** [app/api/billing/webhook/route.ts:55-56](../../app/api/billing/webhook/route.ts:55).
- **Issue:** `user_id` is pulled from `custom_data` and passed straight to `.update(...).eq('id', userId)`. If a legitimate but stale event carries a non-UUID string (test-mode payloads use synthetic IDs; some LS UI actions inject their own custom fields), Postgres raises `22P02 invalid input syntax for type uuid` and the handler returns a 500. LS then re-retries the same broken event forever until manually acked in the LS dashboard.
- **Acceptance:** Closed 2026-07-07. [app/api/billing/webhook/route.ts:71-74](../../app/api/billing/webhook/route.ts:71) — `uuidSchema.safeParse(userId)` (Zod) runs after `user_id` extraction and before the DB path. Failing shape returns `200 { ok: true, ignored: 'invalid user_id' }` (LS stops retrying) + `console.warn` with the offending value + event name. Verified by [\_\_tests\_\_/billing-webhook.test.ts](../../__tests__/billing-webhook.test.ts) X9 tests — `'not-a-uuid-at-all'` returns 200 without touching either `BillingWebhookEvent` or `User`.

#### [x] X10. `resetUser()` is never called on logout — PostHog `distinct_id` persists across sign-outs
- **Where:** [lib/analytics/posthog.ts:36-39](../../lib/analytics/posthog.ts:36) exports `resetUser`; no call site in the logout handler ([app/api/auth/logout/route.ts](../../app/api/auth/logout/route.ts) or wherever sign-out is invoked).
- **Issue:** When user A signs out and user B signs in on the same browser, PostHog keeps A's `distinct_id` cookie until `identifyUser(B.id)` is called on the next successful sign-in. If B just navigates around while signed-out (browsing the landing page, reading `/privacy`), those anonymous events get merged into user A's funnel. In the worst case — a shared machine at an office, an internet café, a family desktop — user A can be re-identified from B's later browsing patterns.
- **Acceptance:** Closed 2026-07-07. `resetUser()` now called before `supabase.auth.signOut()` in both sign-out call sites: [components/header.tsx:60-70](../../components/header.tsx:60) (main header dropdown → "התנתק") and [components/profile/tab-security.tsx:160-165](../../components/profile/tab-security.tsx:160) (account-deletion cleanup path). No dedicated `/api/auth/logout` route exists in this project — sign-out is client-side via the browser Supabase client, so the reset happens at the same call sites.

#### [x] X11. HMAC signature comparison throws on odd-length hex without a clear log
- **Where:** [app/api/billing/webhook/route.ts:29-40](../../app/api/billing/webhook/route.ts:29).
- **Issue:** `Buffer.from(signatureHeader, 'hex')` silently truncates on odd-length input. `timingSafeEqual` then throws `ERR_CRYPTO_TIMING_SAFE_EQUAL_WRONG_LENGTH`, the surrounding try/catch swallows it, and the route returns "invalid signature". Behaviour is correct but observability is bad: a mis-configured LS webhook secret and a caller sending garbage look identical in the logs.
- **Acceptance:** Closed 2026-07-07. [app/api/billing/webhook/route.ts:29-38](../../app/api/billing/webhook/route.ts:29) — `/^[0-9a-f]{64}$/i.test(signatureHeader)` guard runs before `Buffer.from(sig,'hex')` + `timingSafeEqual`. Three distinct log paths: `missing X-Signature header` (401), `malformed X-Signature length=<n>` (401), `invalid signature` (401 — real HMAC mismatch). Verified by [\_\_tests\_\_/billing-webhook.test.ts](../../__tests__/billing-webhook.test.ts) — 4/4 signature tests passing (missing, odd-length hex, non-hex, wrong-key valid-shape).

#### [x] X12. `/api/healthz` reveals internal env-key shape and DB reachability to unauthenticated callers
- **Where:** `app/api/healthz/route.ts` (deleted) + `proxy.ts` public-route allowlist (updated).
- **Issue:** Returned `{ db: 'ok'|'fail', env: 'ok'|'fail' }` publicly with no auth. `env: 'ok'` specifically signalled that `FLEX_TOKEN_ENCRYPTION_KEY` was a 64-character lowercase-hex value — a fingerprint that would confirm to an attacker they hit the right target during recon. `db: 'ok'` revealed Supabase reachability from Vercel edge, useful signal for a targeted DoS or an "is the target awake" probe.
- **Acceptance:** Owner decision 2026-07-07 — endpoint has zero live consumers (verified against `.github/workflows/*.yml`, `vercel.json`, repo grep, and the Vercel dashboard: Deployment Protection has no healthcheck slot, Rolling Releases is disabled + Pro-only). Vercel's platform monitoring + Sentry cover the failure modes an ops person would want a healthcheck for. Fixed by deleting `app/api/healthz/route.ts` entirely and removing the `isHealthz` branch from `proxy.ts` allowlist. `FLEX_TOKEN_ENCRYPTION_KEY` fingerprint no longer reachable from an unauthenticated caller. If a real uptime monitor is added later (UptimeRobot / BetterUptime / a customer SLA requirement), the endpoint can be rebuilt with an `x-healthcheck-secret` header check — ~30 lines of code.

#### [x] X13. `/og` edge route re-renders on every request — cheap cost-amplification vector
- **Where:** [app/og/route.tsx:1-6](../../app/og/route.tsx:1) — `runtime = 'edge'`, no `revalidate`, no `Cache-Control` header on the response.
- **Issue:** `ImageResponse` computes ~50–100 ms of edge CPU per request. The URL is publicly linked from every OG tag (`app/layout.tsx:47,58`), so any social preview or scraper fetches it, and a hostile actor can trivially fire thousands of requests per minute — Vercel bills for edge compute. There's no user-driven input, so the output is byte-identical every call, meaning long cache TTL is safe.
- **Acceptance:** Closed 2026-07-07. [app/og/route.tsx:6-12,229](../../app/og/route.tsx:6) — added `CACHE_HEADERS` constant with `Cache-Control: public, max-age=31536000, s-maxage=31536000, immutable` and passed it via the third arg (`ImageResponseOptions.headers`) to `ImageResponse`. Output is fully deterministic (no query params, no request-scoped state) so `immutable` is safe. Verified post-deploy by `curl -I https://tradeanalyst.app/og` twice — second call returns `x-vercel-cache: HIT`.

#### [x] X14. Webhook update on a non-existent user is a silent no-op instead of an alerted anomaly
- **Where:** [app/api/billing/webhook/route.ts:71-91](../../app/api/billing/webhook/route.ts:71). Overlaps with X5 above but stands alone.
- **Issue:** If the LS webhook carries a `user_id` that doesn't exist in the `User` table (account deleted, test-mode event, race between account deletion and final `subscription_expired` event), the `.update(...).eq('id', userId)` returns success with zero rows affected. The handler continues, writes an `AuditEvent` for a user that doesn't exist, and returns 200 to LS. The billing↔user divergence never surfaces.
- **Acceptance:** Closed 2026-07-07. [app/api/billing/webhook/route.ts:104-122](../../app/api/billing/webhook/route.ts:104) — User update uses `.select('id')` and asserts one row. Zero-row path emits `console.error` with user_id + event + lsSubscriptionId, writes an `AuditEvent { eventType: 'subscription_orphaned', status: 'failure' }` (new type added to `AuditEventType` union in [lib/audit/log.ts](../../lib/audit/log.ts)), and returns `200 { ok:true, ignored: 'user_not_found' }` so LS stops retrying. Verified by X14 test in [\_\_tests\_\_/billing-webhook.test.ts](../../__tests__/billing-webhook.test.ts) — mocked `update.select` returns `[]` → 200 + orphan audit fires.

---

### Phase 3 — Polish (consistency / hygiene)

#### [x] X15. `.env*` was added to `.gitignore` only in the current uncommitted change — verify one more time no secret leaked
- **Where:** working-tree diff on [.gitignore](../../.gitignore) adds `.env*`.
- **Issue:** `git log --all --diff-filter=A -- .env .env.local .env.production` returned only `.env.example`, so no historical leak exists. But the `.gitignore` add wasn't in place during the 28–29 June window; if any developer had a `.env.local` sitting in their tree during those two days and staged it (e.g., via `git add -A`), it would have been silently ignored by nothing. Worth a one-time scan.
- **Acceptance:** Closed 2026-07-08. `git log --all --full-history --oneline -- '.env' '.env.local' '.env.production' '.env.development'` returns empty across the entire repo history. `.gitignore` already covers `.env*` (added in the `e5583f5` launch prep commit). No secret rotation needed.

#### [x] X16. `LAUNCH_PROMO_END` date is duplicated across the client bundle and the server checkout code
- **Where:** [lib/billing/lemon-squeezy.ts:11](../../lib/billing/lemon-squeezy.ts:11) has the canonical `LAUNCH_PROMO_END = new Date('2026-08-01T00:00:00Z')`, and [components/profile/tab-billing.tsx:23](../../components/profile/tab-billing.tsx:23) hardcodes the same date locally.
- **Issue:** A single-source-of-truth violation — if the owner extends the promo, one date will get updated and the other won't, and users will see promo pricing on the profile UI that the server-side checkout no longer applies. Also: the client-side `isLaunchPromo()` runs off the user's clock, so a user with a wrong system clock sees inconsistent UI vs. checkout behavior. This is a launch-window trust issue, not a security one, but it's easy to fix.
- **Acceptance:** Closed 2026-07-08. New [lib/billing/prices.ts](../../lib/billing/prices.ts) owns `LAUNCH_PROMO_END` + `isLaunchPromoActive()` (lemon-squeezy.ts re-exports them for backward-compat). [app/(dashboard)/profile/page.tsx](../../app/(dashboard)/profile/page.tsx) computes `isLaunchPromoActive()` server-side and passes as prop through `ProfileLayout` → `TabBilling` — no client-clock drift. `grep -rn '2026-08-01' components/ app/ lib/` returns hits only in `prices.ts`.

#### [x] X17. `console.log` in production paths leaks broker query IDs
- **Where:** [app/api/ibkr/connect/route.ts:44](../../app/api/ibkr/connect/route.ts:44) — `console.log(\`[ibkr/connect] Saving connection: queryId=${flexQueryIdActivity}\`)`.
- **Issue:** Not new to this window (predates it) but the Sentry wiring now sends structured Node logs to Sentry via `captureRequestError` and Vercel keeps stdout indefinitely — the Flex query ID isn't secret but is user-identifiable and shouldn't be sitting in log storage. Low risk, worth removing.
- **Acceptance:** Closed 2026-07-08. Both `console.log` lines in [app/api/ibkr/connect/route.ts](../../app/api/ibkr/connect/route.ts) removed — the "Saving connection: queryId=..." line (which leaked the Flex query ID) and the "Background sync started" line (redundant noise). Cron paths already use `redactUserId(...)` for userId logs (`app/api/cron/ibkr-sync/route.ts`). Note: `app/api/cron/massive-prices/route.ts` still logs a raw userId, but that route is disabled per CLAUDE.md and out of scope for X17; if massive-prices is ever re-enabled the log should get `redactUserId` too.

#### [x] X18. Pricing strings duplicated across three files with hand-copied values
- **Where:** [app/page.tsx:130-147](../../app/page.tsx:130), [components/billing/pro-required-banner.tsx:26](../../components/billing/pro-required-banner.tsx:26), [components/profile/tab-billing.tsx:13-21](../../components/profile/tab-billing.tsx:13). All hardcode `$14.99` / `$149.99` / `$9.99` / `$99.99`.
- **Issue:** Not a security bug per se, but a price-consistency risk during a launch. The current working-tree diff already shows a bug of exactly this shape — the file `pro-required-banner.tsx` was updated from `$19.99` → `$14.99` in a separate commit than the landing page. If the owner rethinks pricing, three files must move together.
- **Acceptance:** Closed 2026-07-08. [lib/billing/prices.ts](../../lib/billing/prices.ts) exports `PRICE_MONTHLY_USD`, `PRICE_ANNUAL_USD`, `PROMO_MONTHLY_USD`, `PROMO_MONTHLY_DURATION_MONTHS`, `PROMO_ANNUAL_USD`. All 3 UI files (`components/profile/tab-billing.tsx`, `components/billing/pro-required-banner.tsx`, `components/landing/pricing-section.tsx`) import from there. `grep -rn '14\.99|149\.99|9\.99|99\.99' components/ app/ lib/` returns hits only in prices.ts (plus a false positive in a `.999` millisecond comment). Price change is now a one-file edit.

#### [x] X19. Audit-log enum drift between TS type and DB CHECK constraint
- **Where:** [lib/audit/log.ts:4-12](../../lib/audit/log.ts:4) adds `tier_upgraded` / `tier_downgraded` / `subscription_updated` to the TS union; DB `AuditEvent` table has no CHECK constraint on `eventType` (verified against migration `20260621170928`).
- **Issue:** The TS union is enforced at compile time inside the app, but any direct DB write or a future test writing a different string will succeed silently. Not exploitable, but the intent is stronger than the schema currently reflects.
- **Acceptance:** Closed 2026-07-08. Migration `20260708091500_add_audit_event_type_check_constraint` adds `CHECK (eventType IN (...9 values...))` on `AuditEvent`. Values kept in lockstep with the `AuditEventType` TS union in [lib/audit/log.ts](../../lib/audit/log.ts) — includes `subscription_orphaned` from X14. Verified live with `DO $$ ... EXCEPTION WHEN check_violation THEN NULL; END $$` block against production DB — CHECK fires on bogus values as designed.

#### [x] X20. Sentry `withSentryConfig` uses `widenClientFileUpload: true` — audit non-`NEXT_PUBLIC_` env-var references in client code
- **Where:** [next.config.mjs:6-13](../../next.config.mjs:6).
- **Issue:** `widenClientFileUpload: true` uploads all client bundles to Sentry — including bundles for authenticated routes. Sentry stores them for source-mapping errors, which is what you want. But the setting also means any accidentally-inlined secret (e.g., a `.env` variable that wasn't `NEXT_PUBLIC_` but was mistakenly imported into a client file) would sit in the Sentry organisation's storage as well as the Vercel edge cache. Not exploitable today, but the failure mode is worth a one-time review: no client-side file imports anything from `process.env` other than `NEXT_PUBLIC_*` keys.
- **Acceptance:** Closed 2026-07-08. `grep -rn "process.env" components/ | grep -v NEXT_PUBLIC_ | grep -v NODE_ENV` returns empty — the only client file that touches `process.env` is [components/posthog-provider.tsx](../../components/posthog-provider.tsx), and it references only `NEXT_PUBLIC_POSTHOG_KEY`, `NEXT_PUBLIC_POSTHOG_HOST`, and `NODE_ENV` (all safe to inline). Non-`NEXT_PUBLIC_*` env vars appear only in server-only files: route handlers, `lib/supabase/admin.ts`, `lib/billing/lemon-squeezy.ts` (server helper), `lib/chat/gemini-client.ts`, `lib/auth/cron-secret.ts`, `lib/log/redact.ts`, `lib/ibkr/encrypt.ts`, `lib/massive/client.ts`, `lib/utils.ts` (server-only `getBaseUrl`), and `lib/supabase/server.ts`. Standing rule documented in [CLAUDE.md](../../CLAUDE.md).

---

## Open Questions / Items Requiring Owner Input

- ~~**Do we ship PostHog session replay at all, and if yes, at what sampling rate?**~~ **Answered 2026-07-07:** Disable for launch (`disable_session_recording: true`), re-enable in a later phase with full masking to observe drop-off points + Free-→-Pro conversion friction. Encoded into **X2** acceptance criterion; re-enable path tracked as **X23**.
- ~~**What is the acceptable dunning window for `past_due` before we downgrade?**~~ **Answered 2026-07-07:** 0 days. Owner's model — user paid for period P; while `active` they get Pro; when the renewal charge fails and status flips to `past_due`, they're immediately Free. Encoded into **X6** acceptance criterion.
- ~~**Should the webhook honour `subscription_paused` as still-Pro or as Free?**~~ **Answered 2026-07-07:** Free while paused, Pro on resume. Owner also wants an in-app pause/resume affordance in `/profile?tab=billing` (not just a passive banner). Encoded as **X22**.
- ~~**What happens on account deletion — do we forward a subscription cancellation to Lemon Squeezy or leave the LS subscription hanging?**~~ **Answered 2026-07-07:** Yes — on account deletion, cancel the LS subscription server-side; if LS requires user action to fully close (payment-method removal, etc.) the deletion flow surfaces those steps to the user. Encoded as **X21**.
- ~~**Sentry Session Replay retention / redaction policy.**~~ **Answered 2026-07-07:** Free/Developer plan — accept whatever the plan default is (30 days for replays on the free tier). Revisit only if we upgrade Sentry plan.
- ~~**PostHog data-residency choice.**~~ **Answered 2026-07-07:** Stay on US region — the `NEXT_PUBLIC_POSTHOG_HOST` env var is already pointed at `https://us.i.posthog.com`, no migration.
- ~~**Is `/api/healthz` used by an external monitor today?**~~ **Answered 2026-07-07:** Verified in four places — (a) no `.github/workflows/*.yml` calls it, (b) `vercel.json` has no healthcheck configured, (c) no repo reference outside the audit doc + `proxy.ts` allowlist, (d) Vercel dashboard checked live: Deployment Protection page has no healthcheck option, Rolling Releases (the feature that would use a healthcheck path) is disabled and is a Pro-plan feature on a Hobby project anyway, no other setting screen exposes a healthcheck path. Owner has no external uptime monitor registered. **X12 is safe to apply as written.**
- ~~**Do we want a `report-to` endpoint for CSP violations wired to Sentry?**~~ **Answered 2026-07-07:** No — delete the report-only header entirely. Solo dev + AI agents, no one to triage CSP violation reports. Encoded into **X4** acceptance criterion (delete branch).

---

## Discovered During Remediation

> Add new findings here as they surface while working through the plan. Same format: `[ ] X##. Title` + Where / Issue / Acceptance.

#### [x] X22. Add in-app pause / resume subscription flow in `/profile?tab=billing`
- **Where:** [components/profile/tab-billing.tsx](../../components/profile/tab-billing.tsx) + a new server action / route (e.g., `app/api/billing/pause/route.ts` and `app/api/billing/resume/route.ts`) that calls the Lemon Squeezy `PATCH /v1/subscriptions/{id}` API with `attributes: { pause: { mode: 'void' } }` and the resume counterpart. Downstream: [lib/billing/tier.ts](../../lib/billing/tier.ts), webhook handler.
- **Issue:** Owner decision 2026-07-07 — surface pause and resume in the app itself rather than forcing users through the LS customer portal. UX contract: paused user immediately becomes Free (loses access to Pro-gated routes: chat, IBKR connect, CSV export), the Billing tab shows "המנוי מושהה — הפעל מחדש" instead of "בטל מנוי", and clicking Resume triggers the LS resume API which fires `subscription_resumed` back through the webhook → `subscriptionStatus='active'` → Pro restored. Security implications: the pause/resume routes are authenticated per-user, rate-limited (bucket `user:${userId}:billing-state`, ≤ 20/hour), and never accept a `user_id` from the client — they read `session.user.id` server-side and look up `lemonsqueezySubscriptionId` from the DB.
- **Acceptance:** Closed 2026-07-08. Two new API routes: [app/api/billing/pause/route.ts](../../app/api/billing/pause/route.ts) + [app/api/billing/resume/route.ts](../../app/api/billing/resume/route.ts). Both are auth-gated, share a rate-limit bucket `user:${id}:billing-state` (20/hr), look up `lemonsqueezySubscriptionId` server-side from the User row (client never sends it), and call new `pauseSubscription`/`resumeSubscription` helpers in [lib/billing/lemon-squeezy.ts](../../lib/billing/lemon-squeezy.ts) that PATCH LS with `attributes: { pause: { mode: 'void' } }` / `attributes: { pause: null }`. `isActiveStatus` already treats `paused` as Free (X6), so the tier flip happens automatically on the next webhook delivery. New `SubscriptionStateControls` component in [components/profile/tab-billing.tsx](../../components/profile/tab-billing.tsx) renders "השהה מנוי" when status is `active`/`on_trial`, "הפעל מנוי מחדש" when `paused`, and hides otherwise. tab-billing also adds a dedicated paused-state card. Post-action reload picks up the fresh server-rendered `subscriptionStatus`. Errors surfaced inline as a red `role="alert"`. **Owner action to verify post-deploy:** pause a test LS subscription, confirm `/research` chat returns `pro_required` within a webhook round-trip, then resume and confirm Pro access is restored.

#### [x] X23. Re-enable PostHog session recording post-launch with full masking + explicit consent
- **Where:** [lib/analytics/posthog.ts](../../lib/analytics/posthog.ts) + [components/posthog-provider.tsx](../../components/posthog-provider.tsx) + [`app/(public)/privacy/page.tsx`](../../app/(public)/privacy/page.tsx).
- **Issue:** **Deferred 2026-07-08 — future work.** Session recording will return as a product-analytics tool for observing drop-off points and Free-→-Pro upgrade friction. Before re-enabling, the masking policy that X2 sidestepped by disabling entirely must be codified: no ticker symbols, no monetary values, no chat text, no names/emails/phones visible in recordings. This finding stays open as a discrete tracking item; the Acceptance below is the spec for when the owner chooses to re-enable.
- **Acceptance:** ~~When re-enabled: (a) `session_recording: { maskAllInputs: true, maskTextSelector: '[data-sensitive], .font-mono, .text-green, .text-red', blockSelector: '[data-block]', maskTextFn: (text) => text.replace(/\$[\d,.]+/g, '$***').replace(/\b[A-Z]{2,5}\b/g, 'TKR') }`, (b) `sampling: 0.1` (10% of sessions, not 100%), (c) explicit disclosure in the privacy page including PostHog as a subprocessor and the masking policy, (d) opt-out affordance in `/profile?tab=display` that sets `posthog.opt_out_capturing()` for that user, (e) verified by watching one own-user recording and confirming zero P&L / ticker text is readable.~~ **Deferred by owner decision 2026-07-08 — not a launch item.** X23 is closed here as a tracking placeholder; the Acceptance above is the spec for future work. Re-open this finding (or write a new one referencing it) when the owner decides to bring session recording back.

#### [x] X21. Account deletion does not cancel the Lemon Squeezy subscription
- **Where:** Account-deletion path (`app/api/auth/delete/route.ts` or equivalent — verify exact location during implementation) and its interaction with [lib/billing/lemon-squeezy.ts](../../lib/billing/lemon-squeezy.ts).
- **Issue:** Owner confirmed 2026-07-07: it's unethical to keep charging a subscription tied to an account that no longer exists in our DB. Currently the deletion flow only removes the `User` row and the Supabase Auth user; the Lemon Squeezy subscription keeps renewing forever, the user keeps getting charged, and there is no in-app trace to reconcile because their account is gone. From a GDPR Article 17 angle this is also weak — we're holding a billing identifier (`lemonsqueezyCustomerId` inside LS's own records) with no linked data subject.
- **Acceptance:** Closed 2026-07-08. New `cancelSubscription(config, subscriptionId)` helper in [lib/billing/lemon-squeezy.ts](../../lib/billing/lemon-squeezy.ts) issues `PATCH /v1/subscriptions/{id}` with `attributes: { cancelled: true }`. Delete-account route ([app/api/auth/delete-account/route.ts](../../app/api/auth/delete-account/route.ts)) now: (1) reads `lemonsqueezySubscriptionId` from the User row before deletion, (2) if present, calls LS PATCH, (3) records outcome in `AuditEvent.metadata.lsCancelled: true|false|null`, (4) returns `{ ok:true, lsCancelWarning }` — if LS failed, `lsCancelWarning` is a Hebrew string instructing the user to cancel manually via LS portal. Client (`components/profile/tab-security.tsx`) surfaces the warning via `window.alert` before completing sign-out. Deletion is never blocked by LS failures. **Owner action to verify post-deploy:** delete one test account with an active LS subscription — confirm LS dashboard shows the subscription cancelled AND `AuditEvent.metadata.lsCancelled === true`.
